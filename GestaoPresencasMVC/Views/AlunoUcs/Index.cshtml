@model IEnumerable<GestaoPresencasMVC.Models.AlunoUc>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdAlunoNavigation.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdUcNavigation.Nome)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdAlunoNavigation.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdUcNavigation.Nome)
            </td>
            <td>

                    @if (ViewBag.AlunoId == item.IdAlunoNavigation.Id)
                    {
                        <a asp-action="Edit" asp-route-id="@item.Id">Edit  </a>
                        <a>|</a>
                    }
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                @* <a asp-action="Delete" asp-route-id="@item.Id">Delete</a> *@
            </td>
        </tr>
}
    </tbody>
</table>
